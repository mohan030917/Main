name: CI

on:
  push:
    branches: [Bookmyshow]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: lts/*
      - name: Install dependencies
        run: npm ci
      - name: Install Playwright Browsers
        run: npx playwright install --with-deps
      - name: Run Playwright tests
        run: npm run test
      - uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: cucumber-report
          path: cucumber-report/
      - name: Send Email
        uses: actions/send-mail@v2
        with:
          server_url: 'https://github.com/mohan030917/Main/edit/Bookmyshow/.github/workflows/action.yaml'
          port: 587
          from: 'your_email@gmail.com'
          to: 'mohan030917@example.com'
          subject: 'Build Successful: Cucumber Report Attached'
          body: 'Your build has been successful. Please find the Cucumber report attached.'
